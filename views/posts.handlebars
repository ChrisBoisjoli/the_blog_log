<section class="posts">
  <h3 class="post-name">{{post.title}}</h3>
  <h4>User {{user.name}}</h4>
  
  <section class="flex-row justify-center">
    {{#each post }}
    {{post_body}}
    {{/each}}
  </section>
</section>